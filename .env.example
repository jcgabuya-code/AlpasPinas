# Copy this file to ".env.local" and paste your deployed Apps Script web app URLs.
# Leave blank/unset to run in local-only mode (data stays in the browser / bundled JSON).
#
# URLs end in /exec — see google/SHEET-SETUP.md and google/ROSTER-SETUP.md.
VITE_BOOKINGS_ENDPOINT=
VITE_ROSTER_ENDPOINT=
