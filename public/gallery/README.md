# Gallery photos

Drop your real photo files here (jpg/png/webp), then reference them in
`src/data/gallery.json` with a path like `"/gallery/your-photo.jpg"`.

Each entry: { id, category ("Training" | "Races" | "Off-water"), src, alt, caption, credit }.
